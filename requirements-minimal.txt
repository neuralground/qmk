# QMK - Minimal Requirements for CI/CD
# Install with: pip install -r requirements-minimal.txt

# Core testing only - for fast CI/CD
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Essential quantum frameworks for tests
qiskit>=1.0.0
qiskit-aer>=0.13.0

# Scientific computing (required by most quantum frameworks)
numpy>=1.24.0
scipy>=1.10.0

# Data validation (used in QVM)
pydantic>=2.0.0
jsonschema>=4.17.0
